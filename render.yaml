services:
  - type: web
    name: fastify-hello-world-example
    runtime: node
    plan: free
    autoDeploy: false
    rootDir: backend
    buildCommand: |
      cd ../frontend && npm install && npm run build
      npm install
    startCommand: node app.js